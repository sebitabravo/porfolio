# Variables de entorno para el proyecto

# URL del sitio (importante para el acortador de enlaces)
SITE_URL=https://sebita.dev/

# Hash SHA256 de la contraseña de administrador para el acortador
# Para generar: https://passwordsgenerator.net/sha256-hash-generator/
ADMIN_PASSWORD_HASH=tu_hash_sha256_aqui

# Configuración opcional de rate limiting
MAX_REQUESTS_PER_MINUTE=100
MAX_CREATION_ATTEMPTS=5
RATE_LIMIT_WINDOW_MS=300000

# Días después de los cuales se eliminan links sin clicks
CLEANUP_DAYS=30

# Dominio base para el acortador (opcional, por defecto usa SITE_URL)
BASE_DOMAIN=https://sebita.dev

# URLs de empresas (opcional)
INACAP_URL=https://www.inacap.cl/
TELSUR_URL=https://www.telsur.cl/

# GitHub repository (opcional)
GITHUB_REPO_URL=https://github.com/sebitabravo/porfolio

# IMPORTANTE:
# - En desarrollo: usa .env.local (ya está en .gitignore)
# - En producción (Vercel): configura en Dashboard > Settings > Environment Variables
# - Variables automáticas de Vercel: VERCEL_URL, NODE_ENV (no configurar manualmente)
# - NUNCA commitees contraseñas reales al repositorio
