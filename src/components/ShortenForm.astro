---
// URL shortening form component
---

<!-- Formulario para crear link corto -->
<div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8 mb-8">
  <form id="shortenForm" class="space-y-6">
    <div>
      <label
        for="url"
        class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
      >
        URL a acortar *
      </label>
      <input
        type="url"
        id="url"
        name="url"
        required
        placeholder="https://ejemplo.com/mi-link-muy-largo"
        class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-white"
      />
    </div>

    <div>
      <label
        for="customPath"
        class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
      >
        Título/Nombre del enlace *
      </label>
      <div class="flex items-center">
        <span
          class="text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-600 px-3 py-3 border border-r-0 border-gray-300 dark:border-gray-600 rounded-l-lg"
        >
          tudominio.com/
        </span>
        <input
          type="text"
          id="customPath"
          name="customPath"
          required
          placeholder="mi-proyecto-github"
          pattern="[a-zA-Z0-9-_]+"
          class="flex-1 px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-r-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-white"
        />
      </div>
      <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">
        Este será el nombre que aparecerá como título y en la URL. Solo letras,
        números, guiones y guiones bajos.
      </p>
    </div>

    <button
      type="submit"
      class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-6 rounded-lg transition-colors duration-200"
    >
      🔒 Acortar Link (requiere contraseña)
    </button>

    <p class="text-xs text-gray-500 dark:text-gray-400 text-center mt-2">
      Se solicitará contraseña de administrador para crear y eliminar links
    </p>
  </form>
</div>
