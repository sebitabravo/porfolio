---
// URL shortening form component
---

<!-- Formulario para crear link corto -->
<div class="relative group">
  <!-- Background gradient effect -->
  <div
    class="absolute -inset-1 bg-gradient-to-r from-secondary-500 to-accent-500 rounded-3xl blur-xl opacity-25 group-hover:opacity-40 transition-opacity duration-500"
  >
  </div>

  <div
    class="relative bg-white/10 backdrop-blur-sm dark:bg-neutral-800/30 rounded-2xl shadow-2xl p-8 border border-white/20 dark:border-neutral-700/50 hover:shadow-3xl transition-all duration-500"
  >
    <form id="shortenForm" class="space-y-8">
      <div class="space-y-2">
        <label
          for="url"
          class="block text-sm font-semibold text-white/90 dark:text-neutral-200 mb-3"
        >
          🌐 URL a acortar *
        </label>
        <input
          type="url"
          id="url"
          name="url"
          required
          placeholder="https://ejemplo.com/mi-link-muy-largo"
          class="w-full px-6 py-4 bg-white/10 backdrop-blur-sm border border-white/30 dark:border-neutral-600/50 rounded-xl focus:ring-2 focus:ring-secondary-500/50 focus:border-secondary-400 dark:bg-neutral-700/50 dark:text-white placeholder-white/60 dark:placeholder-neutral-400 text-white transition-all duration-300 hover:bg-white/15"
        />
      </div>

      <div class="space-y-2">
        <label
          for="customPath"
          class="block text-sm font-semibold text-white/90 dark:text-neutral-200 mb-3"
        >
          ✨ Título/Nombre del enlace *
        </label>
        <div
          class="flex items-center bg-white/5 backdrop-blur-sm rounded-xl border border-white/20 overflow-hidden hover:bg-white/10 transition-all duration-300"
        >
          <span
            class="text-white/70 dark:text-neutral-300 bg-gradient-to-r from-secondary-500/20 to-accent-500/20 px-4 py-4 border-r border-white/20 font-mono text-sm"
          >
            tudominio.com/
          </span>
          <input
            type="text"
            id="customPath"
            name="customPath"
            required
            placeholder="mi-proyecto-github"
            pattern="[a-zA-Z0-9-_]+"
            class="flex-1 px-4 py-4 bg-transparent border-0 focus:ring-0 focus:outline-none text-white placeholder-white/50 dark:placeholder-neutral-400"
          />
        </div>
        <p
          class="text-xs text-white/60 dark:text-neutral-400 mt-2 leading-relaxed"
        >
          Este será el nombre que aparecerá como título y en la URL. Solo
          letras, números, guiones y guiones bajos.
        </p>
      </div>

      <button
        type="submit"
        class="group relative w-full bg-gradient-to-r from-secondary-600 via-accent-600 to-secondary-800 hover:from-secondary-700 hover:via-accent-700 hover:to-secondary-900 text-white font-semibold py-4 px-8 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-2xl hover:shadow-secondary-500/25 border border-secondary-500/30"
      >
        <span class="relative z-10 flex items-center justify-center space-x-2">
          <span>🔒</span>
          <span>Acortar Link</span>
          <span class="text-sm opacity-75">(requiere contraseña)</span>
        </span>
        <div
          class="absolute inset-0 bg-gradient-to-r from-secondary-500 to-accent-500 rounded-xl opacity-0 group-hover:opacity-20 transition-opacity duration-300"
        >
        </div>
      </button>

      <p
        class="text-xs text-white/50 dark:text-neutral-500 text-center leading-relaxed"
      >
        Se solicitará contraseña de administrador para crear y eliminar links
      </p>
    </form>
  </div>
</div>
